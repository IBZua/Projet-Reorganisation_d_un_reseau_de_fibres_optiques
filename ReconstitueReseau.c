#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "Chaine.h"
#include "Reseau.h"

int main(int argc, char const *argv[])
{
    FILE *f = fopen(argv[1],"r");
    if (atoi(argv[2]) == 1){
        //Methode liste


    }
    if (atoi(argv[2]) == 2){
        //Methode hachage
        

    }
    if (atoi(argv[2]) == 1){
        //Methode arbre
        

    }
    fclose(f);
    return 0;
}
